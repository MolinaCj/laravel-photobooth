import React from 'react';
import ReactDOM from 'react-dom/client';
import PhotoBooth from './components/PhotoBooth';

ReactDOM.createRoot(document.getElementById('photo-root')).render(
    <React.StrictMode>
        <PhotoBooth />
    </React.StrictMode>
);
